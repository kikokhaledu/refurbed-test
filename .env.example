# -----------------------------------------------------------------------------
# Single source of configuration defaults for:
# - docker compose via `make up` / `make down` / `make logs` / `make pre-push`
# - backend runtime (when env vars are provided)
# - frontend runtime (container + optional local overrides)
# - Playwright E2E (through playwright.config.js env loading)
# -----------------------------------------------------------------------------

# Backend service runtime
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8080
BACKEND_DATA_DIR=data
BACKEND_CACHE_TTL_SECONDS=30
BACKEND_CORS_ALLOW_ORIGIN=*

# Frontend service runtime
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=5173
# Keep blank to derive from window host + VITE_BACKEND_PORT.
# Set explicitly only when frontend must call a different origin.
VITE_API_BASE_URL=
# Optional override; if empty, compose falls back to BACKEND_PORT.
VITE_BACKEND_PORT=

# Playwright local test server bindings
PW_BACKEND_HOST=127.0.0.1
# Dedicated backend port for Playwright webServer (kept separate from Compose by default).
PW_BACKEND_PORT=18080
PW_FRONTEND_HOST=127.0.0.1
PW_FRONTEND_PORT=4173
# Optional override; if empty, Playwright frontend uses PW backend base URL.
PW_VITE_API_BASE_URL=
